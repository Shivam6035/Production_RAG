'irm https://astral.sh/uv/install.ps1 | iex'   -- run this in terminal to install packages
'uv init' -- run this to initialize the projects
'uv venv' -- to create virtual environment
'.\.venv\Scripts\activate' -- activate environment

'uv add langchain langchain-core langgraph langchain-openai langchain-anthropic python-dotenv' 
                 -- install requirements and packages


'$null > .env' -- create .env file to save API Key
'uv run main.py' -- to run 

shift + c -- to clear terminal 

create and run main.py to check for API working and all

perfrom document_loader.py to load document  from web scraping
run raw_emb  to check for vecrtorize the data and see output

## ---------------     setup VectorDB on chromaDB


create main.py and test for embedding
we will perfrom similarity search using vector_stores.py

text_splitter runed
token cost runned


#---- Setup LangSmith --- 
and pefrom observablity and tracing

# --- semantic chunking --  
     chunking file 
   uv add langchain-openai langchain chromadb langchain-experimental (necessary file)
   
# advance RAG and pipeline
   adv_rag file 

# production hosting and understanding

       login to 'superbase' and setup

       copy and save all necessary details 

       go to dashborad login and > database > extnsions > enable vector


cousre link :-  https://www.youtube.com/watch?v=mHxLXzYjQRE&t=196s 
          (need to complete till 6hr , after 6 hr advance RAG skip for now)
